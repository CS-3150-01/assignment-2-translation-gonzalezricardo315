#include <stdio.h>

struct Node
{
    int data;
    struct node *node;
};
    
class SinglyLinkedList
{
    public: node *head;
    public: node *tail;
    public: linked_list()
    {
        head = NULL;
        tail = NULL;
    }

    void addNode(int n)
    {
        node *temp = new nodel
        temp->data = n;
        temp->next = NULL;

        if(head == null)
        {
            head = temp;
            tail = temp;
        }
        else 
        {
            tail->next =temp;
            tail = tail->temp;
        }
    }
};
    

int main() {
    linked_list sList;
    a.
    return 0;
}